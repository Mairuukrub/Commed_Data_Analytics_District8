# บริบทโปรเจกต์ (แนบไฟล์นี้ในแชตใหม่)

## เป้าหมาย
ทำ Jupyter notebook (Python) วิเคราะห์ข้อมูล HDC ของ รพ.สต.บ้านเหล่า (hcode 04276) ต.บ้านเหล่า อ.บ้านฝาง จ.ขอนแก่น
เป็น dashboard ภาพรวมชุมชน และเจาะลึก 3 โรค: เบาหวาน (DM), ความดันโลหิตสูง (HT), โรคปอด (COPD)
ผู้ใช้เป็นนักศึกษาแพทย์ อยากเขียนโค้ดเองทีละขั้น ให้ผู้ช่วยสอนและอธิบายเหตุผลทุกขั้น

## สภาพแวดล้อม
Windows, VS Code, Python 3.11 (Anaconda), โฟลเดอร์งาน `C:/Users/User/Desktop/Code/Commed`

## ไฟล์ข้อมูล (โฟลเดอร์ `hdc_full/`)
- `hdc_04276_long.csv` ตารางยาว 1 แถว = 1 ค่า (เฉพาะแถวของ 04276 หรือระดับพื้นที่)
  คอลัมน์: category, subcatalog, report_no, report_code, report_label, fiscal_year_be, view, block, block_label,
  datecom, scope, row_code, row_name, column, value
- `fetch_log.csv` ผลดึงแต่ละรายงาน/ปี (status: ok / empty / error / no_unit_row)
- `report_meta.csv` หัวตารางดิบของรายงาน (cheader, ccolumn) ใช้ถอดความหมายคอลัมน์
- `xlsx/…` ไฟล์ Excel รายรายงาน ชีตละปี มีทั้ง 04276 และ 13896 (รพ.สต.บ้านแดง) + แถวรวม
- ปีงบประมาณ พ.ศ. 2567, 2568, 2569 (ต.ค.–ก.ย.) ปี 2569 ยังไม่ครบปี (ข้อมูลถึงราวกลาง ก.ย. 2569)

## ข้อควรรู้ก่อนเขียนโค้ด
1. `value` เป็นข้อความ ต้อง `pd.to_numeric(errors="coerce")`
2. รายงานเดียวกันอาจซ้ำหลายหมวด ต้อง `drop_duplicates(["report_code","fiscal_year_be","block","row_code","column"])`
3. `scope = unit` คือแถวของ 04276 เอง, `scope = area` คือระดับตำบล (เช่น ปิรามิดประชากร) ห้ามตีความเป็นของหน่วยเดียว
4. CSV นี้ไม่มีข้อมูลบ้านแดง (13896) ถ้าต้องการต้องรันสคริปต์ดึงข้อมูลใหม่ด้วย `--hosp 13896`
5. ข้อมูลรายปี 3 ปีเท่านั้น บอกแนวโน้มไม่ได้ ประชากรหน่วยราว 4,200 คน ตัวเลขแกว่งง่าย ให้ดูจำนวนคนคู่กับอัตราและใช้ช่วงความเชื่อมั่น (Wilson 95%)
6. Python ปรับรูปตัวอักษรไทยบางตัวในชื่อ argument (เช่น `dict(จำนวน=1)` กลายเป็น "จํานวน") ให้ใช้ `{"จำนวน": 1}`
7. กราฟภาษาไทยบน Windows: ตั้งฟอนต์ Tahoma หรือ Leelawadee UI ผ่าน `plt.rcParams["font.family"]`

## ความหมายคอลัมน์ดิบ (ยืนยันจากหัวตารางเว็บ HDC)
- `target` = ฐาน (B), `result` = จำนวนเหตุการณ์ (A), `F3` = ร้อยละหรืออัตรารวม (`result/target*100`)
- รายงานอัตราป่วย/ป่วยตาย: `target1..5`, `result1..5` = กลุ่มอายุ <15, 15-39, 40-49, 50-59, 60+ ปี
- รายงานป่วยรายใหม่: `result1..12` = รายเดือน ต.ค. ถึง ก.ย., `F3` = อัตราต่อแสนประชากร
- ปิรามิดประชากร: `male`, `female`, `total` ต่อกลุ่มอายุ
- TYPEAREA: `type1c..type5c` = จำนวนหลังคลีนข้อมูล ประเภท 1-5

## รายงานที่ใช้ (รหัสรายงาน 6 ตัวแรก ใช้ `report_code.str.startswith(...)`)
| หัวข้อ | DM | HT | COPD |
|---|---|---|---|
| ผู้ป่วยสะสม (อัตราป่วย, แยกอายุ) | cefa42 | 6b9af4 | 33b1c3 |
| ผู้ป่วยรายใหม่ (ต่อแสน, รายเดือน) | eeeab2 | 29eec7 | 62cdb7 |
| ป่วยตาย (ตายจากทุกสาเหตุ ฐานคนละชุดกับผู้ป่วยสะสม) | 589248 | e46c73 | 3bcec2 |
| คัดกรอง อายุ 35+ (target, result, normal, risk, high_risk) | 323a75 | 683312 | - |
| ควบคุมโรค | 137a72 (target, hba1c, result) | 2e3813 (target, no_bp, result, result_bp2) | - |
| อื่นๆ | | | ถุงลมโป่งพอง 265ac8, กำเริบเฉียบพลัน 8dc31d |

อื่นๆ: ปิรามิดประชากร db4e8d (scope=area), ประชากรรายกลุ่มอายุแยกหน่วย 710884, TYPEAREA cb4e5a

## ผลที่ควรตรวจซ้ำ
- ป่วยตายปี 2569 สูงกว่าสองปีก่อนมาก (DM 6 → 2 → 17, HT 7 → 2 → 30 ราย) ควรตรวจกับข้อมูลต้นทาง
- ความครอบคลุมคัดกรองปี 2569 ราว 96–97% เทียบ 45–69% ก่อนหน้า อาจมาจากการนิยามเปลี่ยน
- COPD กำเริบเฉียบพลันเป็น 0 ทุกปี ผู้ป่วย COPD สะสมมีเพียง 21–41 คน
- ตัวอย่างค่าเทียบ: DM ผู้ป่วยสะสม 420/451/465 คน (10.1/11.0/11.0%), HT 682/710/748 คน

## การตรวจสอบที่ทำไปแล้ว
เทียบตัวเลขใน CSV/Excel กับข้อมูลที่ดึงตรงจาก HDC ซ้ำอีกรอบ สำหรับรายงานปิรามิดประชากรและ 710884 ทั้ง 3 ปี ตรงกันทุกค่า
รายงานอื่นยังไม่ได้เทียบทีละค่า ควรสุ่มเทียบกับหน้าเว็บ HDC เอง
